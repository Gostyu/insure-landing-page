import React from 'react';
import Button from './Button';
export default function ContentSectionButton({text}){
    return <Button className="btn contentSection_btn contentSection_btn--center">{text}</Button>
}